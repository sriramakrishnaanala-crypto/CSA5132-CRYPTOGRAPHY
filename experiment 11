#include <stdio.h>

unsigned long long factorial(int n) {
    unsigned long long f = 1;
    for(int i = 2; i <= n; i++)
        f *= i;
    return f;
}

int main() {
    unsigned long long fact25 = factorial(25);
    unsigned long long fact5 = factorial(5);

    printf("25! = %llu\n", fact25);
    printf("Effectively Unique Keys = 25! / (5! * 5!) = %llu\n",
           fact25 / (fact5 * fact5));

    printf("\nApproximate powers:\n");
    printf("25! ≈ 2^84\n");
    printf("Effective keys ≈ 2^70\n");

    return 0;
}
